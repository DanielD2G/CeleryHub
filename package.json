{
  "name": "celeryhub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"bun run --filter @celeryhub/server dev\" \"bun run --filter @celeryhub/web dev\"",
    "build": "bun run --filter @celeryhub/web build && bun run --filter @celeryhub/server build",
    "start": "bun run --filter @celeryhub/server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
